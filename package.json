{
  "name": "competitor-monitor-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrations:apply": "wrangler d1 migrations apply competitor-monitor-dashboard --remote",
    "d1:migrations:apply:local": "wrangler d1 migrations apply competitor-monitor-dashboard --local"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
